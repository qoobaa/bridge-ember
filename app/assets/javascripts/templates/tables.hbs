<h2>Tables</h2>
<ul>
  {{#each controller}}
    <li>
      {{#linkTo "table" id}}{{name}}{{/linkTo}}
    </li>
  {{/each}}
</ul>

<button class="btn" {{action createTable}}>Create a new table</button>
